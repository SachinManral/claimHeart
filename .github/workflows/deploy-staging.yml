name: deploy-staging

on:
  push:
    branches: [develop]

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4
      - name: Build and push staging images
        run: |
          echo "Build/push backend and frontend images tagged ${GITHUB_SHA}"
      - name: Deploy to staging
        run: |
          echo "Deploying to staging..."
      - name: Smoke test
        run: |
          echo "Run staging smoke tests here"
